import 'package:flutter/material.dart';

class Desc2 extends StatelessWidget {
  const Desc2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Heading")),
    );
  }
}
